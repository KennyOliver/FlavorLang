create factorial(n) {
    show "Called with n:", n;

    if n <= 1:
        deliver 1;
    else:
        deliver n * factorial(n - 1);
}

let result = factorial(10);
show "Factorial of 10 is:", result;